// Shared mock data used by RewardsPage and CheckoutPage.
// Replace with real API calls when the backend is wired up.
export const MOCK_BALANCE = 420;
export const POINTS_TO_DOLLAR = 100;
export const MOCK_BOOKING_TOTAL = 289.0;
